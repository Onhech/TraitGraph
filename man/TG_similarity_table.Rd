% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/TG_similarity_table_1.0.R
\name{TG_similarity_table}
\alias{TG_similarity_table}
\title{Create a Psychological Similarity Table}
\usage{
TG_similarity_table(
  dataset,
  columns,
  name = "names",
  format = "matrix",
  threshold = 0.5
)
}
\arguments{
\item{dataset}{A data frame (or tibble) containing the data to plot.}

\item{columns}{A character vector of column names to use for the similarity
calculation (traits/opinions measured for each person).}

\item{name}{The name of the column containing unique identifiers (e.g., full names).
Defaults to \code{"names"}.}

\item{format}{The desired output format. Can be \code{"matrix"} (the default) to
return a raw correlation matrix, or \code{"html"} to return a formatted
\code{knitr_kable} object.}

\item{threshold}{Numeric. The absolute correlation value above which numbers will be
bolded and colored. Defaults to \code{0.5}.}
}
\value{
A correlation matrix (\code{matrix}) or a formatted table (\code{knitr_kable}).
}
\description{
Computes and presents the psychological similarity between individuals as a
correlation matrix. Optionally, it can return a beautifully formatted table
using the \code{kableExtra} package.
}
\examples{
\dontrun{
df <- data.frame(
   names = c("Ada Lovelace", "Grace Hopper", "Marie"),
   favourite_color = c("#1f77b4", "#ff7f0e", "#2ca02c"),
   TraitA = c(3, 4, 5),
   TraitB = c(1, 2, 1),
   TraitC = c(5, 3, 4)
)

# Get a raw correlation matrix (default)
TG_similarity_table(
   dataset = df,
   columns = c("TraitA", "TraitB", "TraitC"),
   name = "names"
)

# Get a formatted HTML table with a different threshold
TG_similarity_table(
   dataset = df,
   columns = c("TraitA", "TraitB", "TraitC"),
   name = "names",
   format = "html",
   threshold = 0.7
)
}

}
