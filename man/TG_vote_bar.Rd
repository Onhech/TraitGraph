% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/TG_vote_bar.R
\name{TG_vote_bar}
\alias{TG_vote_bar}
\title{Create a Simple Vote Bar Chart}
\usage{
TG_vote_bar(
  dataset,
  vote_column,
  name = "name",
  palette = list(high = "#1F7A1F", mid = "#E6E6E6"),
  max_bars = 5,
  show_title = FALSE,
  title = NULL,
  bar_width = 0.6,
  label_size_single = 5,
  label_size_tie2 = 5,
  label_size_tien = 5,
  label_color = NULL,
  tie_markers = c("†", "‡", "§", "¶", "*"),
  font_family = "sans",
  marker_font_family = "Arial Unicode MS",
  footnote_marker_gap = NULL,
  marker_x_nudge = NULL,
  marker_y_nudge = 0.25,
  marker_size = 3.2,
  footnote_x_nudge = NULL,
  footnote_y_nudge = 0.3,
  callout_x_nudge = 0.2,
  callout_y_nudge = 0,
  callout_size = 5,
  footnote_size = 3.5,
  footnote_color = "#3C3C3C",
  output_path = "vote_bar.jpg",
  output_width = 7,
  output_height = 5,
  output_dpi = 300,
  save_plot = FALSE,
  show_plot = TRUE
)
}
\arguments{
\item{dataset}{A data frame containing the vote data.}

\item{vote_column}{Column name containing vote counts for the question.}

\item{name}{Column name containing respondent names. Defaults to "name".}

\item{palette}{A list with \code{high} and \code{mid} hex codes used for the bar gradient.
\code{mid} represents 0 votes and \code{high} represents the maximum votes. Defaults to
\code{list(high = "#1F7A1F", mid = "#E6E6E6")}.}

\item{max_bars}{Maximum number of unique vote counts to display. Defaults to 5.}

\item{show_title}{Logical; if TRUE, include the title in the plot.}

\item{title}{Optional title string.}

\item{bar_width}{Numeric; width of the bars (0-1). Smaller values increase spacing.
Defaults to 0.6.}

\item{label_size_single}{Numeric; label size for single-name rows. Defaults to 5.}

\item{label_size_tie2}{Numeric; label size for two-way ties. Defaults to 5.}

\item{label_size_tien}{Numeric; label size for 3+ way ties. Defaults to 5.}

\item{label_color}{Hex color for labels. Defaults to bar colors when NULL.}

\item{tie_markers}{Character vector of footnote markers to cycle through.
Defaults to \code{c("†", "‡", "§", "¶", "*")}.}

\item{font_family}{Font family for labels and callouts. Defaults to "sans".}

\item{marker_font_family}{Font family for tie markers and footnote markers.
Defaults to "Arial Unicode MS".}

\item{footnote_marker_gap}{Numeric; horizontal gap between marker and text (vote units).
Defaults to \code{0.06 * max_votes}.}

\item{marker_x_nudge}{Numeric; horizontal nudge for tie markers relative to the
label position (in vote units). Defaults to \code{0.015 * max_votes}.}

\item{marker_y_nudge}{Numeric; vertical nudge for tie markers relative to the
label position (in row units). Defaults to 0.25.}

\item{marker_size}{Numeric; font size for tie markers. Defaults to 3.2.}

\item{footnote_x_nudge}{Numeric; horizontal nudge for footnotes (vote units).
Defaults to \code{-0.26 * max_votes}.}

\item{footnote_y_nudge}{Numeric; vertical nudge for footnotes (row units).
Defaults to 0.3.}

\item{callout_x_nudge}{Numeric; horizontal nudge for vote callouts (vote units).
Defaults to 0.2.}

\item{callout_y_nudge}{Numeric; vertical nudge for vote callouts (row units).
Defaults to 0.}

\item{callout_size}{Numeric; font size for vote callouts. Defaults to 5.}

\item{footnote_size}{Numeric; font size for footnotes. Defaults to 3.5.}

\item{footnote_color}{Hex color for footnotes. Defaults to "#3C3C3C".}

\item{output_path}{File path for saving the plot.}

\item{output_width}{Output width in inches.}

\item{output_height}{Output height in inches.}

\item{output_dpi}{Output resolution.}

\item{save_plot}{Logical. If TRUE, save the plot.}

\item{show_plot}{Logical. If TRUE, print the plot.}
}
\value{
Invisibly returns a list with the ggplot and footnote strings.
}
\description{
Generates a horizontal bar chart for a single vote question, showing up to
five unique vote counts with tie-aware labels and optional footnotes.
}
\examples{
\dontrun{
TG_vote_bar(
  dataset = group_dataset,
  vote_column = "vote_q01",
  palette = list(high = "#2F7D32", mid = "#E6F4E6"),
  show_title = FALSE
)
}
}
