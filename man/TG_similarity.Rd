% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/TG_similarity_1.0.R
\name{TG_similarity}
\alias{TG_similarity}
\title{Create a Psychological Similarity Network Graph}
\usage{
TG_similarity(
  dataset,
  columns,
  name = "names",
  color = "favourite_color",
  use_initials = TRUE,
  connection_threshold = 0.3,
  min_node_size = 8,
  max_node_size = 25,
  name_size_mod = 0,
  zoom_out_factor = 1.2,
  title = "Psychological Similarity Network",
  subtitle = "Node size reflects average similarity to others.",
  output_path = "similarity_plot.jpg",
  output_width = 12,
  output_height = 8,
  output_dpi = 300,
  save_plot = TRUE,
  show_plot = TRUE
)
}
\arguments{
\item{dataset}{A data frame (or tibble) containing the data to plot.}

\item{columns}{A character vector of column names to use for the similarity
calculation (traits/opinions measured for each person).}

\item{name}{The name of the column containing unique identifiers (e.g., full names).
Defaults to \code{"names"}.}

\item{color}{The name of the column containing hex color codes (e.g., \verb{#AABBCC})
used to color nodes. Defaults to \code{"favourite_color"}.}

\item{use_initials}{Logical. If \code{TRUE}, nodes are labeled with initials
(first + last initial when available; falls back to first letter for single names).}

\item{connection_threshold}{Numeric in \verb{[0, 1]}. Only edges with absolute
correlation above this value are drawn. Defaults to \code{0.3}.}

\item{min_node_size}{The minimum size for a node, corresponding to the lowest
possible average similarity (-1). Defaults to \code{8}.}

\item{max_node_size}{The maximum size for a node, corresponding to the highest
possible average similarity (+1). Defaults to \code{25}.}

\item{name_size_mod}{Numeric value added to the base label font size (6).}

\item{zoom_out_factor}{Numeric scaling for plot limits (prevents clipping).
Defaults to \code{1.2} (20\% margin).}

\item{title}{Plot title.}

\item{subtitle}{Plot subtitle.}

\item{output_path}{File path for saving the plot (e.g., \code{"similarity_plot.jpg"}).}

\item{output_width}{Width (inches) for the saved image.}

\item{output_height}{Height (inches) for the saved image.}

\item{output_dpi}{DPI for the saved image.}

\item{save_plot}{Logical. If \code{TRUE}, save the plot to \code{output_path}.}

\item{show_plot}{Logical. If \code{TRUE}, print the plot.}
}
\value{
A \code{ggraph} plot object (returned invisibly).
}
\description{
Visualize psychological similarity between individuals in a group as a
network graph. Similarity is computed as the correlation between people
across the selected trait columns. Node size represents the average similarity
of a person to their connections, with larger nodes indicating higher average
similarity. Thicker/greener edges indicate stronger positive similarity, and
redder edges indicate dissimilarity.
}
\examples{
\dontrun{
df <- data.frame(
  names = c("Ada Lovelace", "Grace Hopper", "Marie Curie"),
  favourite_color = c("#1f77b4", "#ff7f0e", "#2ca02c"),
  TraitA = c(5, 1, 4),
  TraitB = c(5, 2, 5),
  TraitC = c(5, 1, 4)
)

TG_similarity(
  dataset = df,
  columns = c("TraitA", "TraitB", "TraitC"),
  connection_threshold = 0.1,
  save_plot = FALSE
)
}

}
