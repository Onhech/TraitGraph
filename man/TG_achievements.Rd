% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/TG_achievements.R
\name{TG_achievements}
\alias{TG_achievements}
\title{Calculate and Allocate Cohort Achievements}
\usage{
TG_achievements(
  trait_map_path,
  vote_map_path,
  data_path,
  achievements_path,
  voting_data_path,
  output_dir = "achievements",
  max_awards = 10,
  write_qa = TRUE,
  soft_fail = TRUE
)
}
\arguments{
\item{trait_map_path}{Path to \code{trait_map.csv}.}

\item{vote_map_path}{Path to \code{vote_map.csv}.}

\item{data_path}{Path to \code{group_dataset.csv}.}

\item{achievements_path}{Path to \code{achievements.csv}.}

\item{voting_data_path}{Path to \code{voting_data.csv}.}

\item{output_dir}{Directory where output CSVs should be written. Defaults to "achievements".}

\item{max_awards}{Integer count of achievements to allocate per person. Defaults to 10.}

\item{write_qa}{Logical; when TRUE, write QA summary CSVs to output_dir.}

\item{soft_fail}{Logical; when TRUE, missing inputs/columns emit warnings and
return empty outputs instead of stopping.}
}
\value{
A list with \code{all_eligible} and \code{awarded} data frames.
}
\description{
Reads cohort data, vote data, and achievement definitions; calculates eligibility,
then allocates a fixed number of achievements per person based on priority and
exclusivity rules. Outputs include a full eligibility list and a final allocation.
}
\examples{
\dontrun{
inputs_dir <- system.file("achievement_inputs", package = "TraitGraph")
TG_achievements(
  trait_map_path = file.path(inputs_dir, "trait_map.csv"),
  vote_map_path = file.path(inputs_dir, "vote_map.csv"),
  data_path = file.path(inputs_dir, "group_dataset.csv"),
  achievements_path = file.path(inputs_dir, "achievements.csv"),
  voting_data_path = file.path(inputs_dir, "voting_data.csv"),
  output_dir = "achievements",
  max_awards = 10
)
}
}
