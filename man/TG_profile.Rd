% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/TG_profile.R
\name{TG_profile}
\alias{TG_profile}
\title{Create a Circular Profile Chart}
\usage{
TG_profile(
  dataset,
  value_column,
  trait_column = "name",
  low_color_column = "Low_Color",
  mid_color_column = "Mid_Color",
  high_color_column = "High_Color",
  palette = NULL,
  title = NULL,
  show_title = TRUE,
  color_opacity = 0.95,
  gradient_lighten = FALSE,
  gradient_lighten_max = 0.75,
  gradient_lighten_power = 2,
  label_radius_base = 110,
  label_nudge_bottom = FALSE,
  title_size_mod = 0,
  title_vjust_mod = 0,
  name_size_mod = 0,
  plot_zoom_mod = 0,
  inner_hole_size_mod = 0,
  margin_y_mod = 0,
  margin_x_mod = 0,
  output_path = "profile_plot.jpg",
  output_width = 7,
  output_height = 5,
  output_dpi = 300,
  save_plot = FALSE,
  show_plot = TRUE
)
}
\arguments{
\item{dataset}{A data frame containing trait rows for a single profile.}

\item{value_column}{The column name containing percentile values.}

\item{trait_column}{The column name containing trait labels. Defaults to "name".}

\item{low_color_column}{Column name for each trait's low color (hex). Defaults to "Low_Color".}

\item{mid_color_column}{Optional column name for each trait's midpoint color (hex). Defaults to "Mid_Color".}

\item{high_color_column}{Column name for each trait's high color (hex). Defaults to "High_Color".}

\item{palette}{Optional fallback palette list with \code{high}, \code{low}, and optional \code{mid} colors.
Used when any row is missing valid colors. Defaults to grayscale fallback.}

\item{title}{A string for the plot's main title.}

\item{show_title}{Logical: if TRUE (default), include the title in the plot.}

\item{color_opacity}{Numeric between 0 and 1 controlling bar transparency. Defaults to 0.95.}

\item{gradient_lighten}{Logical; if TRUE, lightens colors toward white near the midpoint (50). Defaults to FALSE.}

\item{gradient_lighten_max}{Numeric between 0 and 1; maximum lightening toward white when \code{gradient_lighten = TRUE}. Defaults to 0.75.}

\item{gradient_lighten_power}{Numeric >= 0 controlling the curve of lightening; values > 1 make lightening drop off faster as scores move away from 50.}

\item{label_radius_base}{Numeric y coordinate used for name labels.}

\item{label_nudge_bottom}{Logical; if TRUE (default) slightly offsets labels near the 6 o'clock position.}

\item{title_size_mod}{A numeric value to add/subtract from the title font size.}

\item{title_vjust_mod}{A numeric value to add/subtract from the title vertical adjustment.}

\item{name_size_mod}{A numeric value to add/subtract from the name label font size.}

\item{plot_zoom_mod}{A numeric value to add/subtract from the outer plot boundary.}

\item{inner_hole_size_mod}{A positive numeric value to reduce the inner hole size.}

\item{margin_y_mod}{A numeric value (in cm) to add/subtract from top/bottom margins.}

\item{margin_x_mod}{A numeric value (in cm) to add/subtract from left/right margins.}

\item{output_path}{The full path where the plot will be saved.}

\item{output_width}{The width of the saved image in inches.}

\item{output_height}{The height of the saved image in inches.}

\item{output_dpi}{The resolution (dots per inch) for the saved image.}

\item{save_plot}{A logical value. If TRUE, the plot is saved to disk.}

\item{show_plot}{A logical value. If TRUE, the plot is displayed.}
}
\value{
Invisibly returns the ggplot object.
}
\description{
Generates a polar bar chart for a single person's psychological profile, where
each row represents a trait and colors are derived from each trait's own
low/high palette.
}
\examples{
\dontrun{
TG_profile(
  dataset = group_score,
  value_column = "group_score",
  trait_column = "name",
  low_color_column = "Low_Color",
  high_color_column = "High_Color",
  title = "Group Psychological Profile",
  show_title = FALSE
)
}
}
